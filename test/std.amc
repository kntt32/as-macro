import core;
import core.syscall;
import std.syscall;

pub extern fn main();

pub fn _start() {
    main();
    let status: i32@rdi;
    status = 0;
    exit(status);
};

pub fn panic_handler() {
    let code: i32@rdi = -1;
    exit(code);
};


