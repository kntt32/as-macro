import std;

static string: [char; 20] = "Hello, World!\n";

pub fn main() {
    let fd: i64@rdi;
    let msg: *char@rsi;
    let len: i64@rdx;

    fd = stdout;
    lea(msg, string[0]);
    len = 14;

    write(fd, msg, len);

    let status: i32@rdi;
    status = 0;
    exit(status);
};



