import std;

static string: [char; 20] = "Hello, World!\n";

pub fn main() {
    let fd: i64@rdi;
    let msg: *char@rsi;
    let len: i64@rdx;

    fd = stdout;
    lea(msg, string);

    let strlen: i64@rcx;
    len = strlen(strlen, msg);

    write(fd, msg, len);
};



