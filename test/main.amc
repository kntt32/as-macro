import std;
import std.io;
import std.io.print;
import std.box;
import std.vec;

impl Box(i64);
impl Display_Box(Io, i64);

impl Vec(i64);

pub as drop(inout self: i64@reg+mem) {};
pub as clone(in self: i64@reg+mem, out buff: i64@reg+mem) {
    buff = self;
};

fn main() {
    let str: Str@stack;
    str.from("Hello, Println!");
    !DEBUG_LABEL;
    let a: i64@rax = str.slice.len;
    println(str);

    let box: defer Box_i64@stack;
    box.new(12345);
    println(box);
};

