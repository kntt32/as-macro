import sub;

struct Point {
    x: i64,
    y: i64
};

as mov(x: i64@reg, y: i64@imm) : (double: 0xb8, rq, iq);

as nop() : (double: 0x90);

pub as NOP() {
    nop();
};

fn f() {
    NOP();
};

pub fn main(x: i64@r15) {
    test();
    mov(x, 10);
    nop();
};

