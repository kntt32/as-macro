import std;
import std.io;
import std.io.print;
import std.vec;

pub as drop(x: i32@reg+mem) {};

fn main() {
    let value: i64@auto = 0;
    for((let i: i64@auto = 0; let flag: bool@auto); flag.lt(i, 1000000); ++i) {
        value += i;
    };
    println(value);
};

