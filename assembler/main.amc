import sub;
import core_x64;

struct Point {
    x: i64,
    y: i64
};

pub as NOP() {
    nop();
};

fn f() {
    NOP();
};

pub fn main(x: i64@r15, y: i64@rax, z: f64@xmm1) {
    mov(x, -1);
    let num: i32@rdi;
    mov(num, 12345);
};

