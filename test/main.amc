import std;
import std.io;

fn main() {
    let fnptr: fn(u32@rax)@rdi;
    fnptr =& test;
    let x: u32@rax = 1;
    (fnptr)(x);
};

fn test(x: u32@rax) {
    println("Hello, FnPtr!");
    printfln(x);
};

