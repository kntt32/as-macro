import std;
import std.io;
import std.vec;

impl Vec(i32);
impl Display_Vec(Out, i32);

fn main() {
    let vec: defer Vec_i32@stack;
    vec.new();

    for((let i: i32@auto = 0; let flag: bool@auto); flag.lt(i, 1024); ++i) {
        vec.push(i);
    };

    printfln(vec);
};
